// SPDX-License-Identifier: ice License 1.0

import {smallHeightDevice} from '@constants/styles';
import {rem} from 'rn-units';

export const EMOTION_IMAGE_SIZE = smallHeightDevice ? rem(80) : rem(120);
export const TAKE_SELFIE_BUTTON_SIZE = smallHeightDevice ? rem(80) : rem(100);
